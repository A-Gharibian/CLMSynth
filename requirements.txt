# Core dependencies for the CLMSynth pipeline (main.py and the clm_label engine).
# Requires Python 3.11+; pinned to the versions used for verification (Python 3.14).
numpy==2.5.2
pandas==3.0.5
PyYAML==6.0.3
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2

# --- Optional ---
# pyivm          # metrics.evaluate_cluster_label_matching (adjusted
# internal-validity measures). NOT IMPLEMENTED YET: provisional hook,
# nothing in the pipeline calls it. The code works without it.
# git+https://github.com/CN-TU/mdcgenpy    # data_source: "mdcgen";
# the code will work without it if configuration files point to other sources.
# faker  # fabricated_data source only; has a numpy fallback if absent
