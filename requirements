# Core dependencies for CLMSynth (main.py and the clm_label engine).
# Requires Python 3.11 or higher; tested on Python 3.14.
numpy==2.5.1
pandas==3.0.3
PyYAML==6.0.3
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2

# --- Optional ---
# pyivm                                    # metrics.evaluate_cluster_label_matching (adjusted internal-validity measure ARI),
# the code will work without if configuration foles point to other measures.
# git+https://github.com/CN-TU/mdcgenpy    # data_source: "mdcgen", 
# the code will work without if configurations files point to other sources.
# faker                                    # dummy source only; has a numpy fallback if absent
